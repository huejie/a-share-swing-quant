export type Action='持有'|'加仓'|'减仓'|'退出'|'待买';
export type Holding={code:string;name:string;theme:string;action:Action;weight:number;initialWeight:number;currentWeight:number;price:number;change:number;score:number;thesis:string[];invalidation:string;initialStop:number;protectivePrice:number|null;entry:string;entryState:string;version:string;timestamp:string;risk:string;days:number;expectedHoldingDays:[number,number];nextReviewAt:string;kind:'model'|'simulation'};
export type Theme={name:string;phase:string;score:number;breadth:number;flow:number;crowding:number;relativeStrength:number;fundamental:number;catalyst:number;leadership:number;note:string};
export type Decision={id:string;date:string;title:string;reason:string;version:string;result:string};
export type SimulatedPosition={symbol:string;shares:number;avgCost:number;updatedAt:string};
export type SimulationEvent={id:string;symbol:string;side:string;status:string;quantity:number;price:number|null;fee:number;createdAt:string;reason:string};
export type EquityPoint={day:string;cash:number;marketValue:number;equity:number;drawdown:number};
export type Simulation={matchingReady:boolean;matchingReason:string;cash:number;marketValue:number;equity:number;drawdown:number;positions:SimulatedPosition[];ledger:SimulationEvent[];dailyEquity:EquityPoint[]};
export type Snapshot={asOf:string;cashWeight:number;portfolioStatus?:'healthy'|'partial'|'risk_off'|'observation';portfolioReason?:string;market:{state:string;score:number;exposure:number;style:string;summary:string;factors:{name:string;score:number;note:string;weight:number}[]};holdings:Holding[];candidates:Holding[];themes:Theme[];decisions:Decision[];equity:number[];simulation:Simulation};
export type DataProvenance={provider:string;mode:'demo'|'public-prototype'|'research'|'verified';pitVerified:boolean;productionReady:boolean;degradations:string[];marketInputs?:{source:string;globalRiskQuality:string;fundFlowQuality:string;valuationQuality:string}};
export type ApiEnvelope<T>={data:T;source:'live'|'demo';stale:boolean;provenance:DataProvenance;message?:string};
